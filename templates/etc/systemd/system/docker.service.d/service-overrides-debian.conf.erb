[Service]
EnvironmentFile=-/etc/default/docker
EnvironmentFile=-/etc/default/docker-storage
<% @extra_systemd_parameters.each do |key, value| -%>
<%= key %>=<%= value %>
<% end -%>
ExecStart=
ExecStart=/usr/bin/<%= @docker_command %> -d -H fd:// $OPTIONS \
        $DOCKER_STORAGE_OPTIONS
